import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rampage.pixelmonster',
  appName: 'Pixel Monster Rampage',
  webDir: 'dist'
};

export default config;
